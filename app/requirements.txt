# ============================================
# LIGHTWEIGHT OCR SERVICE - REQUIREMENTS
# ============================================
# Total size: ~85-105MB (with QR support)
# Last updated: 2025-10-14
# ============================================

# Core FastAPI Framework (Required)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Data Validation (Required)
pydantic==2.5.0

# OCR Engine - Tesseract (Required)
# Note: Also requires system Tesseract binary
# Install: sudo apt install tesseract-ocr tesseract-ocr-eng tesseract-ocr-hin
pytesseract==0.3.10

# Image Processing (Required)
Pillow==10.1.0
numpy==1.24.3

# OpenCV - Headless version (no GUI = smaller size)
# Using headless: ~30MB vs regular opencv-python: ~50MB
opencv-python-headless==4.8.1.78

# QR Code Scanning (NEW - for 100% accurate Aadhaar extraction)
# ~5MB - Reads QR codes from Aadhaar cards
pyzbar==0.1.9

# PDF Processing (Optional - only if you need PDF support)
# Comment out if you only process images
PyMuPDF==1.23.8

# Development & Testing (Optional)
# Uncomment for development
# pytest==7.4.3
# pytest-asyncio==0.21.1
# httpx==0.25.2

# ============================================
# REMOVED (for lightweight version):
# ============================================
# paddlepaddle==2.5.2        # ❌ Removed (300MB)
# paddleocr==2.7.3           # ❌ Removed (100MB)
# opencv-python==4.8.1.78    # ❌ Replaced with headless version
#
# Total savings: ~420MB (80% reduction)
# ============================================

# ============================================
# SYSTEM DEPENDENCIES REQUIRED:
# ============================================
# Ubuntu/Debian:
#   sudo apt update
#   sudo apt install tesseract-ocr tesseract-ocr-eng tesseract-ocr-hin
#   sudo apt install libzbar0  # For QR code scanning
#
# CentOS/RHEL:
#   sudo yum install tesseract tesseract-langpack-eng tesseract-langpack-hin
#   sudo yum install zbar  # For QR code scanning
#
# macOS:
#   brew install tesseract tesseract-lang
#   brew install zbar  # For QR code scanning
#
# Windows:
#   Tesseract: Download from https://github.com/UB-Mannheim/tesseract/wiki
#   ZBar: pip install pyzbar (includes DLLs for Windows)
# ============================================